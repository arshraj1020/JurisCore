target/
!.mvn/wrapper/maven-wrapper.jar
build/
out/

*.class
*.jar
*.war
*.log

.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
nbproject/

.DS_Store
Thumbs.db

# Local overrides and anything that could hold a real secret.
.env
.env.*
!.env.example
application-local-override.yml
*.pem
*.p12
*.jks

# Frontend
node_modules/
frontend/dist/
*.tsbuildinfo

# Archives and scratch output. Thirty megabytes of build tarballs were committed once
# because nothing here excluded them; *.jar was ignored but a tar full of jars was not.
*.tgz
*.tar
*.tar.gz
*.zip
# The JVM drops these beside a module when a debugger or agent attaches during tests.
.attach_pid*
# Assistant/scratch output folders. Anything worth keeping belongs in docs/.
Claude outputs/
